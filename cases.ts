let person_name: string= "shary";
console.log(`lowercase:${person_name. toLowerCase()}`);
console.log(`uppercase:${person_name. toUpperCase()}`);
console.log(person_name.charAt(0).toUpperCase() + person_name.slice(1).toLowerCase()); 