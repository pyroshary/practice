let user: string[] = ["abc"];

//using if length plus else
if(user.length === 0){console.log("we dont have any user right now!")}
else{console.log(`hey ${user},how can i help you?`)};

let user1: string[] = [];
//using if length plus else
if(user1.length === 0){console.log("we dont have any user right now!")}
else{console.log(`hey ${user1},how can i help you?`)};
